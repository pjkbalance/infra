# TYPE  DATABASE        USER            ADDRESS                 METHOD

# 允许本地 Unix 套接字连接
local   all             all                                     trust
# 允许所有地址使用密码认证访问
host    all             all             0.0.0.0/0               scram-sha-256

# 仅允许特定地址范围访问
# host    all             all             192.168.1.0/24          scram-sha-256
# 示例: 允许特定用户通过特定 IP 使用特定数据库
# host    raus            user            192.168.1.100/32        md5